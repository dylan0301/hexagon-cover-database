\section{Introduction and Proof Outline}
\label{sec:introduction}

\subsection{The theorem and the canonical labeling}

Let
\[
 O=(0,0),\qquad
 V_i=\left(\cos\frac{i\pi}{3},\sin\frac{i\pi}{3}\right),\qquad
 H=\conv\{V_0,\ldots,V_5\},
\]
where indices lie in $\mathbb Z/6\mathbb Z$.  Write
\[
 e_{i,i+1}=[V_i,V_{i+1}],\qquad r_i=[O,V_i],\qquad M_i=\frac12V_i,
\]
and put
\[
 S=\partial H\cup\bigcup_{i=0}^5r_i.
\]
Then
\[
 \Haus(\partial H)=6,
 \qquad
 \Haus\left(\bigcup_{i=0}^5r_i\right)=6,
 \qquad
 \Haus(S)=12.
\]

\begin{theorem}[Main theorem]
\label{thm:main}
The hexagon $H$ cannot be covered by seven open equilateral triangles of side
length one.
\end{theorem}

\begin{corollary}[Soifer's Hexagon Problem]
\label{cor:expanded-closed}
For every $L>1$, the regular hexagon $H_L=LH$ cannot be covered by seven closed
unit equilateral triangles.
\end{corollary}

The seven points $O,V_0,\ldots,V_5$ are pairwise at distance at least one,
whereas any two points in an open unit equilateral triangle are at distance
strictly less than one.  Hence the seven covering triangles admit a unique
labeling
\[
 U_C,U_0,\ldots,U_5
\]
such that
\[
 O\in U_C,
 \qquad
 V_i\in U_i.
\]
We call $U_C$ the \emph{center triangle} and $U_i$ the \emph{vertex triangle at
$V_i$}.  Set
\[
 T_C=\overline{U_C},\qquad T_i=\overline{U_i}.
\]

\begin{figure}[H]
\centering
\input{figures/geometry_roles}
\caption{The center, vertices, radial segments, and radial midpoints of $H$.}
\label{fig:geometry-roles}
\end{figure}

\subsection{Finite classifications}

The center type is the number of boundary edges on which $T_C$ has a
positive-length trace.  The only possibilities are $0,1,2$, denoted by
$\CEzero,\CEone,\CEtwo$.

For a vertex triangle, let $o$ be the number of its triangle vertices outside
$H$ and let $n$ be the number of adjacent radial segments met in positive
length.  The exhaustive types are
\[
\begin{array}{c|c}
\text{type}&(o,n)\\ \hline
\Vdzero&(1,0),(2,0),(3,0)\\
\Vdone&(1,1)\\
\Vdtwo&(1,2)\\
\Tlike&(2,1).
\end{array}
\]

Let $A_i,B_i,C_i$ be the maximal reaches of $T_i$ toward
$V_{i-1},V_{i+1},O$, respectively.  The vertex triangle $T_i$ is
\emph{supercritical} when $A_i+B_i>1$, and
\[
 N_+=\left\lvert\left\{i:A_i+B_i>1\right\}\right\rvert.
\]
Lowercase $(a_i,b_i,c_i)$ always denotes selected lower bounds for these
reaches and never the maxima defining $N_+$.

On $e_{i,i+1}$, parametrized from $V_i$ to $V_{i+1}$, the two incident open
traces have the form $[0,B_i)$ and $(1-A_{i+1},1]$.  Their nonempty complement
\[
 [B_i,1-A_{i+1}]
\]
is a \emph{boundary gap}; equality gives a singleton gap, which remains
uncovered.  Let $N_{\mathrm{gap}}$ be the number of positive center traces
that contain a boundary gap.

Let $N_{\mathrm{sp}}$ be the number of vertex triangles of type
$\Vdone,\Vdtwo,$ or $\Tlike$.  The common trace calculation proves that the
number of vertex triangles that are either supercritical or meet an adjacent
radial segment in positive length is
\[
 N_++N_{\mathrm{sp}}.
\]
In particular, every CE1/CE2 configuration with
$N_++N_{\mathrm{sp}}\ge3$ is impossible by a trace-length bound on $S$.

\subsection{Exhaustive routing}

Table~\ref{tab:routing} is read from top to bottom in the CE1/CE2 part.  The
first applicable row is the unique method for a hypothetical cover.  The full
classification proof is Proposition~\ref{prop:exhaustive-structural-reduction}.

\begin{table}[H]
\centering
\scriptsize
\renewcommand{\arraystretch}{1.07}
\begin{tabular}{p{.25\textwidth}p{.55\textwidth}c}
\toprule
center type and $N_+$&exhaustive refinement&method\\
\midrule
$\CEzero$, $N_+=0$&all vertex types&1\\
$\CEzero$, $N_+=1$&all six triangles $\Vdzero$&4\\
$\CEzero$, $N_+=1$&a $\Vdone$ or $\Vdtwo$ triangle&1\\
$\CEzero$, $N_+=1$&no $\Vdone/\Vdtwo$ triangle and a $\Tlike$ triangle&3\\
$\CEzero$, $N_+\ge2$&all vertex types&3\\
\midrule
$\CEone/\CEtwo$&$N_++N_{\mathrm{sp}}\ge3$&1\\
$\CEone/\CEtwo$, $N_+=0$&all six triangles $\Vdzero$&2\\
$\CEone/\CEtwo$, $N_+=0$&a $\Vdone$ or $\Vdtwo$ triangle&1\\
$\CEone/\CEtwo$, $N_+=0$&no $\Vdone/\Vdtwo$ triangle and a $\Tlike$ triangle&2\\
$\CEone/\CEtwo$, $N_+=1$&all $\Vdzero$, no boundary gap&4\\
$\CEone/\CEtwo$, $N_+=1$&all $\Vdzero$, a boundary gap&2\\
$\CEone/\CEtwo$, $N_+=1$&exactly one $\Tlike$, no $\Vdone/\Vdtwo$&2\\
$\CEone/\CEtwo$, $N_+=1$&exactly one $\Vdone/\Vdtwo$, no $\Tlike$&
  $1\;/\;1{+}2$\\
\bottomrule
\end{tabular}
\caption{The exhaustive routing.  Method 1 is a trace-length argument, Method
2 propagates reach bounds, Method 3 sums area loss, and Method 4 uses a
nine-point enclosure obstruction.  In the last row, the first alternative is
for CE1 and the second for CE2.}
\label{tab:routing}
\end{table}

\subsection{Organization of the proof}

Section~\ref{sec:structure-common} proves the structural classification,
strict handoff selection, midpoint facts, and the signed center normal form.
Section~\ref{sec:trace-length} gives the complete trace-length argument.
Section~\ref{sec:boundary-propagation} develops the local admissible set and
all boundary-reach propagation cases.  Section~\ref{sec:area-loss} proves the
local and cyclic area-loss estimates.  Section~\ref{sec:nine-point} constructs
the nine forced points and proves the equilateral-enclosure obstruction.
Section~\ref{sec:assembly} then applies Table~\ref{tab:routing} once and closes
the proof.
