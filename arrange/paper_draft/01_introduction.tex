\section{Proof Architecture}
\label{sec:introduction}

This section gives the complete logical map before any technical verification.
The body defines every object used in the proof, records the exact outputs of
the calculations, explains the four geometric mechanisms, and performs the
final case assembly.  The TeX appendices and formally incorporated electronic
supplements prove the stated formulas and inequalities.  Thus a reader can
follow the contradiction from the body alone and consult the verification
modules only to check a calculation or a finite branch certificate.

\subsection{The theorem and the seven forced roles}

Let
\[
 O=(0,0),\qquad
 V_i=\left(\cos\frac{i\pi}{3},\sin\frac{i\pi}{3}\right),\qquad
 H=\conv\{V_0,\ldots,V_5\},
\]
where indices lie in $\mathbb Z/6\mathbb Z$.  Write
\[
 e_{i,i+1}=[V_i,V_{i+1}],\qquad r_i=[O,V_i],\qquad M_i=\frac12V_i,
\]
and put
\[
 D=\bigcup_{i=0}^5r_i,\qquad S=\partial H\cup D.
\]
Then $\Haus(\partial H)=\Haus(D)=6$ and $\Haus(S)=12$.

\begin{theorem}[Main theorem]
\label{thm:main}
The hexagon $H$ cannot be covered by seven open equilateral triangles of side
length one.
\end{theorem}

The compactness and scaling argument in
Proposition~\ref{prop:open-closed-scaled} gives the equivalent closed version.

\begin{corollary}[Expanded closed formulation]
\label{cor:expanded-closed}
For every $L>1$, the regular hexagon $H_L=LH$ cannot be covered by seven closed
unit equilateral triangles.
\end{corollary}

The seven points $O,V_0,\ldots,V_5$ are pairwise at distance at least one.
Consequently a hypothetical open cover has seven distinct roles: an open
triangle $U_C$ containing $O$ and open triangles $U_i$ containing $V_i$.
Their closures are denoted by $T_C,T_i$.

\begin{figure}[H]
\centering
\input{figures/geometry_roles}
\caption{The center anchor, six vertex anchors, boundary edges, and radial
arms.  The role labels are forced; triangle orientations are not.}
\label{fig:geometry-roles}
\end{figure}

\subsection{The finite dictionary}

The center type is the number of boundary edges on which $T_C$ has a
positive-length trace.  The only possibilities are $0,1,2$, denoted by
$\CEzero,\CEone,\CEtwo$.

For a vertex role, let $o$ be the number of its triangle vertices outside $H$
and let $n$ be the number of adjacent radial arms met in positive length.  The
exhaustive types are
\[
\begin{array}{c|c}
\text{type}&(o,n)\\ \hline
\Vdzero&(1,0),(2,0),(3,0)\\
\Vdone&(1,1)\\
\Vdtwo&(1,2)\\
\Tlike&(2,1).
\end{array}
\]

For an original vertex role, let $A_i,B_i,C_i$ be its actual maximal incoming
boundary, outgoing boundary, and own-radial reaches.  The V triangle is
\emph{supercritical} when $A_i+B_i>1$, and
\[
 N_+=\left\lvert\left\{i:A_i+B_i>1\right\}\right\rvert.
\]
Lowercase $(a_i,b_i,c_i)$ always denotes selected lower demands and never the
actual maxima defining $N_+$.

On $e_{i,i+1}$, parametrized from $V_i$ to $V_{i+1}$, the two incident open
traces have the form $[0,B_i)$ and $(1-A_{i+1},1]$.  Their complement
\[
 [B_i,1-A_{i+1}]
\]
is a boundary gap; equality gives a singleton gap, which is still uncovered.
For CE1/CE2, let $\mathrm{gr}$ be the number of positive center traces
containing such a gap.

Call a vertex role \emph{short} if it is supercritical or has positive-length
trace on an adjacent arm.  Let $m$ count the roles of type
$\Vdone,\Vdtwo,\Tlike$, and let $q$ count short roles.  The structural
calculus gives
\[
 q=N_++m.
\]

\subsection{The exhaustive routing table}

Table~\ref{tab:routing} is read from top to bottom in the CE1/CE2 part.  The
first applicable entry is the unique route for the hypothetical cover.  The
structural proof is summarized in Proposition~\ref{prop:body-structural-reduction}
and given in full in Appendix~\ref{sec:structural-reductions}.

\begin{table}[H]
\centering
\scriptsize
\renewcommand{\arraystretch}{1.07}
\begin{tabular}{p{.25\textwidth}p{.55\textwidth}c}
\toprule
center and V-triangle count&exhaustive refinement&route\\
\midrule
$\CEzero$, $N_+=0$&all vertex types&1\\
$\CEzero$, $N_+=1$&all six roles $\Vdzero$&4\\
$\CEzero$, $N_+=1$&a $\Vdone$ or $\Vdtwo$ role&1\\
$\CEzero$, $N_+=1$&no $\Vdone/\Vdtwo$ role and a $\Tlike$ role&3\\
$\CEzero$, $N_+\ge2$&all vertex types&3\\
\midrule
$\CEone/\CEtwo$&$q\ge3$&1\\
$\CEone/\CEtwo$, $N_+=0$&all six roles $\Vdzero$&2\\
$\CEone/\CEtwo$, $N_+=0$&a $\Vdone$ or $\Vdtwo$ role&1\\
$\CEone/\CEtwo$, $N_+=0$&no $\Vdone/\Vdtwo$ role and a $\Tlike$ role&2\\
$\CEone/\CEtwo$, $N_+=1$&all $\Vdzero$, zero gaps&4\\
$\CEone/\CEtwo$, $N_+=1$&all $\Vdzero$, a positive gap&2\\
$\CEone/\CEtwo$, $N_+=1$&exactly one $\Tlike$, no $\Vdone/\Vdtwo$&2\\
$\CEone/\CEtwo$, $N_+=1$&exactly one $\Vdone/\Vdtwo$, no $\Tlike$&
  $1\;/\;1{+}2$\\
\bottomrule
\end{tabular}
\caption{The complete terminal routing.  Route $j$ means Strategy $j$.  In the
last entry, the first alternative is for CE1 and the second for CE2.}
\label{tab:routing}
\end{table}

For the two all-$\Vdzero$ CE1/CE2 routing entries, the same six cells are organized by
\[
\begin{array}{c|ccc}
 &\mathrm{gr}=0&\mathrm{gr}=1&\mathrm{gr}=2\\ \hline
N_+=0&\text{strict identity cycle}&\text{one-side endpoint chain}&
       \text{paired endpoint chain}\\
N_+=1&\text{nine-point support}&\text{five-V-triangle chain}&
       \text{paired endpoint chain}.
\end{array}
\]
The last column is CE2-only.

\subsection{Four mechanisms}

Strategy~1 bounds one-dimensional traces on $\partial H$ or $S$.
Strategy~2 propagates lower reaches with monotone maps obtained from one local
admissible set.  Strategy~3 sums forced area losses.  Strategy~4 forces nine
points into the center role and applies an equilateral enclosure obstruction.
Sections~\ref{sec:strategy1-summary}--\ref{sec:strategy4-reader} contain these
arguments; Section~\ref{sec:assembly} performs the final audit.
