\section{Strategy 2: Relaxations of \(g\)-Composition Chains}
\label{sec:strategy2-reader}

The signed center section supplies the exact CE1/CE2 traces, exits, and
actual-row interfaces.  Section~\ref{sec:universal-calculus} supplies one
transfer alphabet:
\[
 g_c,\qquad \widehat g_c,\qquad
 g_c^\vee,\qquad \widehat g_c^\vee,
\]
together with center-assisted subscripts and decorated lower relaxations.
Every proof in this section starts from an exact row chain and retains only
the lower transfers needed for its terminal contradiction.  Complete
contact-cell calculations remain in
Appendix~\ref{sec:exact-local-demand-calculus}, where the technical aliases
$B_c,F_c,G_c$ are retained when they shorten algebra.

\subsection{The common chain table}

For maps listed in geometric row order,
\[
 [\Phi_1\mid\cdots\mid\Phi_r](x)
 =
 (\Phi_r\circ\cdots\circ\Phi_1)(x).
\]
The following table records the seed, exact chain skeleton, relaxed slots, and
terminal certificate separately.  No additional branch-signature symbol is
needed.

\begin{table}[H]
\centering
\scriptsize
\renewcommand{\arraystretch}{1.12}
\begin{tabular}{@{}p{.17\textwidth}p{.235\textwidth}p{.235\textwidth}p{.235\textwidth}@{}}
\toprule
branch&exact skeleton&relaxations used&terminal certificate\\
\midrule
$N_+=0$, all $\Vdzero$, $\mathrm{gr}=0$&
cyclic six-row $[\widehat g_{C_i}^\vee]$ chain&
$\mathrm I^6$&
strict cyclic ascent\\
$N_+=0$, all $\Vdzero$, $\mathrm{gr}=1$&
two exact hatted outgoing endpoint caps and three middle rows&
middle $\mathrm I^3$&
$\widehat g_{c_1}(1-q)+\widehat g_{c_5}(1-s)<1$\\
$\CEtwo$, all $\Vdzero$, $\mathrm{gr}=2$, $N_+=0$ or $1$&
paired exact endpoint caps and three middle rows&
middle $\mathrm I^3$&
$\widehat g_{p/W}(1-p)+\widehat g_{q/R}(1-q)<1$\\
$N_+=0$, one or more $\Tlike$, no $\Vdone/\Vdtwo$&
two residual endpoint caps and three middle rows&
middle $\mathrm I^3$&
exact four-label endpoint sum $<1$\\
$\CEone$, $N_+=1$, all $\Vdzero$, one gap&
$[\widehat g_{c_1}^\vee\mid\cdots\mid\widehat g_{c_5}^\vee](X)$&
two affine superscripts and one threshold superscript&
returned demand $>1-X$\\
$\CEtwo$, $N_+=1$, all $\Vdzero$, one gap&
the same five-row chain&
one threshold-decorated slot and four identity slots&
returned demand $>1-H$\\
one $\Tlike$ or adjacent $\Vdone$ rescuer&
free supercritical source and ordinary path&
seed $1-g_c^{\rm sc}$, then $\mathrm I^3$&
$b_5<g_c^{\rm sc}\le h$\\
adjacent $\Vdone/\Vdtwo$ placement&
center residuals $A,H$ and backward ordinary chain&
backward $\mathrm I^4$&
$\widehat g_{1-\delta}^\vee(1/2+A)>1-H$\\
nonadjacent $\Vdone/\Vdtwo$ placement&
center residuals and intervening ordinary rows&
identity propagation&
Vd-specific terminal radial separation\\
Vd1--supercritical replacement&
one exceptional adjacent pair&
replace by two nonsupercritical $\Vdzero$ rows&
invoke the relevant all-$\Vdzero$ chain\\
\bottomrule
\end{tabular}
\caption{All Strategy~2 chain relaxations in the canonical \(g\)-family.}
\label{tab:strategy2-chain-signatures}
\end{table}

The hard T3-like endpoint calculation is not replaced by a weaker unsupported
formula.  Likewise, the nonadjacent Vd terminal remains type-specific rather
than a universal $\widehat g_c^\vee$ inequality.  The existing Strategy~1
perimeter and skeleton routes, including the two Strategy~1 subcases of the
one-Vd1/Vd2 package, retain their statements and routing.

\subsection{The all-\(\Vdzero\) gap-rank kernel}

Let $\mathrm{gr}$ be the number of positive center traces that contain a
V-gap.  Then
\[
 \mathrm{gr}\in\{0,1,2\},
 \qquad
 \mathrm{gr}\le1\quad\text{for }\CEone.
\]
The two all-$\Vdzero$ row counts are governed by
{\small
\[
\begin{array}{c|ccc}
 &\mathrm{gr}=0&\mathrm{gr}=1&\mathrm{gr}=2\\ \hline
N_+=0&
\text{strict identity cycle}&
\text{one-side endpoint chain}&
\text{paired endpoint chain}\\
N_+=1&
\text{nine-point obstruction}&
\text{five-row relaxed chain}&
\text{paired endpoint chain}.
\end{array}
\]
}
The last column is CE2-only.

For $N_+=0$ and $\mathrm{gr}=0$, the six open vertex traces alone cover each
boundary edge.  Hence
\[
 A_{i+1}>1-B_i.
\]
Since every row is nonsupercritical,
\[
 B_i\le\widehat g_{C_i}(1-A_i),
\]
and therefore
\[
 A_{i+1}
 >
 1-B_i
 \ge
 1-\widehat g_{C_i}(1-A_i)
 =
 \widehat g_{C_i}^\vee(A_i)
 \ge A_i.
\]
Iteration gives
\[
 A_0<A_1<\cdots<A_5<A_0,
\]
the strict identity-cycle contradiction.

For $N_+=0$ and $\mathrm{gr}=1$, the one-side theorem keeps the two endpoint
caps exact:
\[
 \widehat g_{c_1}(1-q)+\widehat g_{c_5}(1-s)<1.
\]
The three middle rows contribute only $\mathrm I^3$, or equivalently
Lemma~\ref{lem:reader-boundary-path} contradicts their three unit caps.  The
same description applies to the T3-like $N_+=0$ branch after replacing the
endpoint inputs by the residuals $A_1,A_5$; only its endpoint inequality
requires the exact four-label audit.

\begin{proposition}[Common CE2 two-gap application]
\label{prop:reader-common-two-gap}
Assume both CE2 center traces contain V-gaps and rows $T_1,\ldots,T_5$ are
nonsupercritical Vd0 rows.  Then the perimeter is not covered.  No hypothesis
on the criticality of $T_0$ is required.
\end{proposition}

\begin{proof}
Put
\[
 p=W-\alpha,
 \qquad
 q=R-\delta.
\]
Gap containment forces the two endpoint inputs $p,q$, while the exact center
exits give radial demands $p/W$ and $q/R$.  Hence
\[
 B_5\le\widehat g_{p/W}(1-p),
 \qquad
 B_1\le\widehat g_{q/R}(1-q).
\]
The exact paired endpoint theorem in
Appendix~\ref{sec:exact-local-demand-calculus} gives
\[
 \widehat g_{p/W}(1-p)+\widehat g_{q/R}(1-q)<1.
\]
Thus $B_1+B_5<1$.  The three middle rows are relaxed to $\mathrm I^3$;
equivalently Lemma~\ref{lem:reader-boundary-path}, applied to
$T_2,T_3,T_4$, forces their total boundary contribution above three, contrary
to their three nonsupercritical caps.
\end{proof}

For $N_+=1$ and $\mathrm{gr}=0$, the active proof remains the direct
nine-point obstruction of Strategy~4.  Thus the table records the all-Vd0
kernel without changing that cell's routing.

\subsection{One signed five-row chain}

Use the signed center data of
Proposition~\ref{prop:signed-center-normal-form}.  Put
\[
 X=R-\delta,
 \qquad
 H=\frac{\eta+\alpha+\delta}{2R},
 \qquad
 m=\min\left\{\frac\alpha R,\frac\delta W\right\},
\]
and
\[
 \begin{aligned}
 c_1&=1-\frac\delta R,&
 c_2&=1-\delta,&
 c_3&=1-m,\\
 c_4&=1-\alpha,&
 c_5&=1-\frac\alpha W.
 \end{aligned}
\]
A gap in the normalized right trace starts the same exact chain for CE1 and
CE2:
\begin{equation}
 Z=
 [\widehat g_{c_1}^\vee
 \mid\widehat g_{c_2}^\vee
 \mid\widehat g_{c_3}^\vee
 \mid\widehat g_{c_4}^\vee
 \mid\widehat g_{c_5}^\vee](X).
 \label{eq:reader-exact-five-chain}
\end{equation}
The actual-row interface in
Proposition~\ref{prop:signed-one-gap-interface} gives
\[
 a_0\ge Z,
 \qquad
 a_0<1-H.
\]
The first and fifth slots are relaxed to $\mathrm I$.  Three applications of
exact capped-map duality reverse the middle chain and reduce both center
classes to
\begin{equation}
 [\widehat g_{1-\alpha}^\vee
 \mid\widehat g_{1-m}^\vee
 \mid\widehat g_{1-\delta}^\vee](H)
 >1-X=W+\delta.
 \label{eq:reader-signed-three-map}
\end{equation}
The only remaining distinction is the sign of
$\Delta_L=P-R\alpha-\delta$.

\begin{proposition}[CE1 one-gap obstruction]
\label{prop:reader-ce1-one-gap}
The CE1, $N_+=1$, all-$\Vdzero$, one-gap state is impossible.
\end{proposition}

\begin{proof}
Here $\Delta_L\le0$.  The low-root estimates give $e(\alpha)<X$ and
$H>\alpha$.  At the row with deficit $\alpha$, the two Full ranges are
impossible.  The $L$ and $T_-$ ranges immediately give an output above
$1-X$.

On the selected $T_+$ range, the center inequalities force $X>1/2$.  The
universal affine relaxations give
\[
 p_1>
 \widehat g_{1-\alpha}^{\vee,\,1-4\alpha}(H)
 =(2-4\alpha)H-(1-4\alpha)\alpha
 =:L_1,
\]
and then
\[
 p_2>
 \widehat g_{1-m}^{\vee,\,1-5m}(L_1)
 =(2-5m)L_1-(1-5m)m
 =:L_2.
\]
The exact terminal calculation in Appendix~\ref{sec:cyclic-all-vd0} proves
\begin{equation}
 \delta<\frac1{10},
 \qquad
 p_2>L_2>2\delta+3\delta^2>e(\delta).
 \label{eq:reader-ce1-terminal}
\end{equation}
Thus the final slot is relaxed to
$\widehat g_{1-\delta}^{\vee,\rm th}$, which produces an output above
$1-X$.  This proves \eqref{eq:reader-signed-three-map}, and the common
actual-row interface gives the contradiction.
\end{proof}

\begin{corollary}[CE2 one-gap obstruction]
\label{prop:reader-ce2-one-gap}
The CE2, $N_+=1$, all-$\Vdzero$, exactly-one-gap state is impossible in either
orientation.
\end{corollary}

\begin{proof}
This is Proposition~\ref{prop:signed-ce2-one-gap}.  Its initial estimate and
two-threshold estimate are
\[
 X=R-\delta>e(\alpha),
 \qquad
 \min\{e(\alpha),e(\delta)\}<H.
\]
If $e(\alpha)<H$, the row-$4$ slot is replaced by
$\widehat g_{1-\alpha}^{\vee,\rm th}$ and every other slot by $\mathrm I$:
\[
 [\mathrm I\mid\mathrm I\mid\mathrm I
 \mid\widehat g_{1-\alpha}^{\vee,\rm th}
 \mid\mathrm I](X)>1-H.
\]
Otherwise $e(\delta)<H\le e(\alpha)<X$, so the row-$2$ slot is replaced by
$\widehat g_{1-\delta}^{\vee,\rm th}$:
\[
 [\mathrm I\mid\widehat g_{1-\delta}^{\vee,\rm th}
 \mid\mathrm I\mid\mathrm I\mid\mathrm I](X)>1-H.
\]
Reflection exchanges $R$ with $W$, $\alpha$ with $\delta$, and reverses the
row order.
\end{proof}

\subsection{The remaining transfer branches}

For $N_+=0$, the all-Vd0 proof combines the strict identity cycle, the
one-side exact endpoint theorem, and
Proposition~\ref{prop:reader-common-two-gap}.  The T3-like branch first
isolates a T3-like row and then applies one exact four-label endpoint sum.  Its
selected high sheet is the universal curve \eqref{eq:reader-psi}, with rational
parameter \eqref{eq:reader-beta-rational}.

For the exactly-one-T3-like $N_+=1$ branch, the translated local profile
proves
\[
 a\le1-g_c^{\rm sc},
 \qquad
 \frac{a}{a+1-u}\le1-g_c^{\rm sc}.
\]
The common adjacent-rescuer theorem then gives
\[
 b_5<g_c^{\rm sc}\le h,
\]
with only $\mathrm I^3$ between the source and terminal comparisons.

For the one-Vd1/Vd2 branch, the preliminary perimeter budget removes CE1 and
gives
\[
 \alpha+\delta<\frac1{24},
 \qquad
 \alpha+\delta<\frac{\min\{R,W\}}6.
\]
The adjacent placement proves $\delta<H/4$ and uses
\[
 \widehat g_{1-\delta}^\vee\left(\frac12+A\right)>1-H.
\]
The nonadjacent placements use the interval residuals and one Vd corner radial
margin.  The remaining Vd1 rescue uses the same
$1-g_c^{\rm sc}$--identity--$g_c^{\rm sc}$ chain as the T3-like branch.

\begin{proposition}[Branches closed by relaxed \(g\)-chains]
\label{prop:reader-demand-branches}
Strategy~2 closes:
\begin{enumerate}
\item the $N_+=0$ all-$\Vdzero$ and T3-like-without-Vd1/Vd2 branches for
      $\CEone$ and $\CEtwo$;
\item every positive-gap $N_+=1$ all-$\Vdzero$ state;
\item the exactly-one-T3-like $N_+=1$ state with no Vd1/Vd2 role;
\item the complementary transfer placements in the $\CEtwo$, $N_+=1$,
      exactly-one-Vd1/Vd2 branch.
\end{enumerate}
Singleton gaps are included.
\end{proposition}

\begin{proof}
The first item is Propositions~\ref{prop:nplus-zero-all-vd0} and
\ref{prop:nplus-zero-t3}.  For the second, the one-gap states are
Proposition~\ref{prop:reader-ce1-one-gap} and
Corollary~\ref{prop:reader-ce2-one-gap}; the two-gap state is
Proposition~\ref{prop:reader-common-two-gap}.  The third item is
Proposition~\ref{prop:nplus-one-one-t3}.  The fourth is
Proposition~\ref{prop:signed-ce2-one-vd-placements}.  Every formal transfer is
connected to actual rows by Lemma~\ref{lem:reader-raw-capped-transfer} or
Lemma~\ref{lem:reader-generalized-handoff}.
\end{proof}
