\section{Strategy 2: Monotone Transfer Certificates}
\label{sec:strategy2-summary}

Strategy~2 turns boundary coverage into lower bounds propagated around the
six V triangles.  The geometric input is Proposition~\ref{prop:body-transfer-interface};
the remaining work is to retain a small number of exact endpoint maps, replace
other center-free nonsupercritical V triangles by lower relaxations, and prove one
terminal inequality.

\paragraph{Signed-center auxiliaries used by the certificates.}
Definition~\ref{def:body-signed-center-data} gives the geometric meaning of the
signed center data.  Appendix~\ref{sec:signed-ce12-reductions} defines the
associated auxiliary functions $W,E,\eta,P,k,\Delta_R,\Delta_L$ and the exact
semialgebraic domain $\mathcal D_{\rm sgn}$.  The reader-facing argument uses
those named functions and domains, but does not repeat their affine equations.

\begin{definition}[Geometric-order composition and identity slots]
\label{def:body-chain-notation}
Let $r\in\mathbb Z_{\ge1}$, let
$X_0,\ldots,X_r$ be sets, and let
$\Phi_j:X_{j-1}\to X_j$ for $1\le j\le r$.  Define
\[
 [\Phi_1\mid\cdots\mid\Phi_r]
 :=\Phi_r\circ\cdots\circ\Phi_1:X_0\to X_r.
\]
For $X_0=\cdots=X_r=[0,1]$, define
\[
 \mathrm I:[0,1]\to[0,1],\qquad \mathrm I(x):=x,
\]
\[
 \mathrm I^j:=
 \underbrace{[\mathrm I\mid\cdots\mid\mathrm I]}_{j\text{ slots}}
 \quad(j\in\mathbb Z_{\ge1}),
 \qquad
 \mathrm I^0:=\mathrm I.
\]
\end{definition}

\begin{figure}[H]
\centering
\resizebox{.88\linewidth}{!}{\input{figures/relaxed_composition}}
\caption{Exact and lower-relaxed states on consecutive hexagon-skeleton V triangles.
The internal edges used by an identity relaxation are center-free; endpoint
center intervals are first absorbed into residual demands.}
\label{fig:transfer-chain}
\end{figure}

\begin{definition}[Signed Strategy 2 parameter domains]
\label{def:body-strategy2-signed-domains}
With the exact signed auxiliary functions and domain from
Appendix~\ref{sec:signed-ce12-reductions}, define
\[
 \mathcal D_{\rm CE1}:=
 \left\{(R,\alpha,\delta)\in\mathcal D_{\rm sgn}:
 \begin{array}{c}
 \Delta_L\le0,\quad
 0<\alpha<W/2,\quad 0<\delta<R/2,\\
 R\delta-W\alpha>0
 \end{array}
 \right\},
\]
\[
 \mathcal D_{\rm CE2}:=
 \left\{(R,\alpha,\delta)\in\mathcal D_{\rm sgn}:
 \Delta_L>0
 \right\}.
\]
\end{definition}

\begin{figure}[H]
\centering
\resizebox{.98\linewidth}{!}{%
\input{figures/strategy2_signed_domains_skeleton}}
\caption{The signed parameter domain and its exact CE1/CE2 Strategy~2
subdomains on the two possible center-trace edges.}
\label{fig:strategy2-signed-domains-skeleton}
\end{figure}

\begin{definition}[Center-free ordinary boundary path]
\label{def:body-center-free-path}
For $r\in\{1,\ldots,5\}$ and
\[
 \mathbf i=(i_0,\ldots,i_r)\in(\mathbb Z/6\mathbb Z)^{r+1},
\]
define
\[
 E_j(\mathbf i):=e_{i_j,i_{j+1}}
 \qquad(1\le j\le r-1),
\]
\[
 \mathfrak P_{\rm cf}(r):=
 \left\{
 \begin{array}{l}
 \mathbf i\in(\mathbb Z/6\mathbb Z)^{r+1}:\\
 i_{j+1}=i_j+1
 \quad\forall j\in\{0,\ldots,r-1\},\\
 U_C\cap E_j(\mathbf i)=\varnothing
 \quad\forall j\in\{1,\ldots,r-1\},\\
 \Haus(U_k\cap E_j(\mathbf i))=0\\
 \qquad\forall j\in\{1,\ldots,r-1\},\
 \forall k\in(\mathbb Z/6\mathbb Z)
 \setminus\{i_j,i_{j+1}\}
 \end{array}
 \right\}.
\]
\[
 \mathfrak P_{\rm cf}:=
 \bigcup_{r=1}^{5}\mathfrak P_{\rm cf}(r).
\]
\end{definition}

\begin{figure}[H]
\centering
\resizebox{.98\linewidth}{!}{%
\input{figures/strategy2_center_free_path_skeleton}}
\caption{A representative element of $\mathfrak P_{\rm cf}(3)$, including
every internal-edge exclusion in the exact center-free ordinary-path set.}
\label{fig:strategy2-center-free-path-skeleton}
\end{figure}

\subsection{The all-Vd0 kernel}

\begin{definition}[V-gaps and active-gap rank]
\label{def:body-active-gap-rank}
For $i\in\mathbb Z/6\mathbb Z$, parametrize $e_{i,i+1}$ by
\[
 \xi_i:[0,1]\to e_{i,i+1},
\qquad
 \xi_i(t):=V_i+t(V_{i+1}-V_i).
\]
The set missed on this edge by its two incident open vertex roles is
\[
 \Gamma_i:=
 \begin{cases}
  \xi_i\bigl([B_i,1-A_{i+1}]\bigr),
       &B_i\le1-A_{i+1},\\
  \varnothing,&B_i>1-A_{i+1}.
 \end{cases}
\]
A nonempty $\Gamma_i$ is a \emph{V-gap}; in particular,
$B_i=1-A_{i+1}$ gives a singleton V-gap.  In a hypothetical cover define
\[
 \mathrm{gr}:=
 \left\lvert
 \left\{i\in\mathbb Z/6\mathbb Z:
 \Gamma_i\ne\varnothing,\ \Gamma_i\subset U_C
 \right\}
 \right\rvert .
\]
Boundary locality implies that every nonempty $\Gamma_i$ is contained in
$U_C$, so $\mathrm{gr}$ is equivalently the number of nonempty V-gaps, or the
number of positive center traces containing one.
\end{definition}

\begin{figure}[H]
\centering
\resizebox{.99\linewidth}{!}{\input{figures/strategy2_gap_rank_skeleton}}
\caption{The three active-gap ranks.  A singleton $\Gamma_i$ remains a
nonempty V-gap and is counted.}
\label{fig:strategy2-gap-rank-skeleton}
\end{figure}

The two V-triangle counts and the active-gap rank form the single kernel
\[
\begin{array}{c|ccc}
 &\mathrm{gr}=0&\mathrm{gr}=1&\mathrm{gr}=2\\ \hline
N_+=0&\mathrm I^6\text{ cycle}&
       \text{two endpoints}&
       \text{paired endpoints}\\
N_+=1&\text{nine points}&
       \text{five-V-triangle chain}&
       \text{paired endpoints}.
\end{array}
\]
The last column is CE2-only.  For $N_+=0$ and $\mathrm{gr}=0$, the center has
no trace on any missed handoff and the six incident vertex traces alone cover
every edge.  Thus
\[
 A_{i+1}>1-B_i
 \ge\widehat g_{C_i}^\vee(A_i)\ge A_i,
\]
which gives the impossible strict cycle
\[
 A_0<A_1<\cdots<A_5<A_0.
\]

\subsection{Exact terminal certificates}

\begin{definition}[One-gap and paired-gap endpoint data]
\label{def:body-gap-endpoint-data}
For
\[
 (R,\alpha,\delta)\in
 \mathcal D_{\rm CE1}\cup\mathcal D_{\rm CE2},
 \qquad
 W:=1-R,\quad
 \eta:=1-\sqrt{1-RW},\quad
 k:=\eta+\alpha+\delta,
\]
define
\[
 a_5^{\mathrm{one}}:=\frac{k}{R},\qquad
 a_1^{\mathrm{one}}:=R-\delta,
\]
\[
 c_5^{\mathrm{one}}:=1-\frac{\alpha}{W},\qquad
 c_1^{\mathrm{one}}:=1-\frac{\delta}{R}.
\]
\[
 (a_5^{\mathrm{one}},a_1^{\mathrm{one}},
   c_5^{\mathrm{one}},c_1^{\mathrm{one}})\in[0,1]^4.
\]
For $(R,\alpha,\delta)\in\mathcal D_{\rm CE2}$, define additionally
\[
 a_5^{\mathrm{two}}:=W-\alpha,\qquad
 a_1^{\mathrm{two}}:=R-\delta,
\]
\[
 c_5^{\mathrm{two}}:=\frac{a_5^{\mathrm{two}}}{W},\qquad
 c_1^{\mathrm{two}}:=\frac{a_1^{\mathrm{two}}}{R}.
\]
\[
 (a_5^{\mathrm{two}},a_1^{\mathrm{two}},
   c_5^{\mathrm{two}},c_1^{\mathrm{two}})\in[0,1]^4.
\]
\end{definition}

\begin{proposition}[All-Vd0 endpoint certificates]
\label{prop:body-all-vd0-endpoints}
\begin{enumerate}
\item Suppose
\[
 (R,\alpha,\delta)\in
 \mathcal D_{\rm CE1}\cup\mathcal D_{\rm CE2}.
\]
Assume $N_+=0$, all six roles are Vd0, and exactly the normalized right
center trace contains a V-gap.  Use the one-gap data of
Definition~\ref{def:body-gap-endpoint-data}.
Then the two exact hatted outgoing caps satisfy
\[
 \widehat g_{c_5^{\mathrm{one}}}(1-a_5^{\mathrm{one}})
 +\widehat g_{c_1^{\mathrm{one}}}(1-a_1^{\mathrm{one}})<1.
\]
The companion edge is center-free in CE1 and gap-free in CE2; the three middle
V triangles satisfy Definition~\ref{def:body-center-free-path} and may be replaced
by $\mathrm I^3$.

\item Suppose
\[
 (R,\alpha,\delta)\in\mathcal D_{\rm CE2}.
\]
Assume the two center traces both contain V-gaps, and V triangles
$T_1,\ldots,T_5$ are nonsupercritical Vd0.  Use the paired-gap data of
Definition~\ref{def:body-gap-endpoint-data}.
Then
\[
 \widehat g_{c_5^{\mathrm{two}}}(1-a_5^{\mathrm{two}})
 +\widehat g_{c_1^{\mathrm{two}}}(1-a_1^{\mathrm{two}})<1.
\]
Again the three middle V triangles satisfy
Definition~\ref{def:body-center-free-path}.
\end{enumerate}
Singleton gaps are included.
\end{proposition}

\begin{proof}
These are the one-side and paired-endpoint theorems in
Appendix~\ref{sec:exact-local-demand-calculus}, applied through the corrected
boundary-path lemma, Lemma~\ref{lem:reader-boundary-path}.  The endpoint
residuals include every center contribution before the ordinary path is used.
\end{proof}

\begin{figure}[H]
\centering
\resizebox{.99\linewidth}{!}{\input{figures/strategy2_endpoint_data_skeleton}}
\caption{The complete one-gap and paired-gap endpoint data, including the
boundary inputs, endpoint radial demands, and center-free middle path.}
\label{fig:strategy2-endpoint-data-skeleton}
\end{figure}

\begin{definition}[Low root and decorated lower relaxations]
\label{def:body-decorated-relaxations}
Set
\[
 d_*:=1-\frac{\sqrt3}{2}.
\]
Define the high-radial low root
\[
 e:(0,d_*)\to(0,1),\qquad
 e(d):=\frac{1-d}{2}
 \left(1-\sqrt{4(1-d)^2-3}\right),
\]
and the low-radial order transition
\[
 h:\left(\frac12,\frac{\sqrt3}{2}\right]\to(0,1),\qquad
 h(c):=\frac{2c}{1+\sqrt{16c^2-3}}.
\]
For $0<d<1/2$, define the selected-$T_+$ input interval
\[
 \mathcal T_d:=[d,\tau(d)],\qquad
 \tau(d):=
 \begin{cases}
  e(d),&0<d<d_*,\\
  h(1-d),&d_*\le d<1/2.
 \end{cases}
\]
Define the set of certified affine coefficients
\[
 \mathcal C_d:=
 \left\{\lambda\in\mathbb R:
 z+\lambda(z-d)\le\widehat g_{1-d}^{\vee}(z)
 \quad\forall z\in\mathcal T_d
 \right\}.
\]
For $\lambda\in\mathcal C_d$, define the affine lower relaxation on its
certified interval by
\[
 \widehat g_{1-d}^{\vee,\lambda}:\mathcal T_d\to\mathbb R,\qquad
 \widehat g_{1-d}^{\vee,\lambda}(z):=z+\lambda(z-d).
\]
For $0<d<d_*$, define the threshold lower relaxation on all of $[0,1]$ by
\[
\widehat g_{1-d}^{\vee,\mathrm{th}}:[0,1]\to[0,1],
\qquad
 \widehat g_{1-d}^{\vee,\mathrm{th}}(z):=
 \begin{cases}
  z,&0\le z\le e(d),\\
  1-e(d),&e(d)<z\le1.
 \end{cases}
\]
It satisfies
\[
 \widehat g_{1-d}^{\vee,\mathrm{th}}(z)
 \le\widehat g_{1-d}^{\vee}(z)\qquad(0\le z\le1).
\]
\end{definition}

\begin{figure}[H]
\centering
\resizebox{.98\linewidth}{!}{%
\input{figures/strategy2_decorated_domain_skeleton}}
\caption{The exact domains $d_*$, $e$, $h$, $\mathcal T_d$, $\tau$, and
$\mathcal C_d$ for the decorated lower relaxations.}
\label{fig:strategy2-decorated-domain-skeleton}
\end{figure}

\begin{figure}[H]
\centering
\resizebox{.98\linewidth}{!}{%
\input{figures/strategy2_decorated_maps_skeleton}}
\caption{The general affine and threshold decorated maps, with their
certification domains, on retained Strategy~2 skeleton V triangles.}
\label{fig:strategy2-decorated-maps-skeleton}
\end{figure}

\begin{definition}[Exact five-V-triangle one-gap data]
\label{def:body-five-V-triangle-data}
For
\[
 (R,\alpha,\delta)\in
 \mathcal D_{\rm CE1}\cup\mathcal D_{\rm CE2},
\]
with $W,\eta,k$ as defined in
Appendix~\ref{sec:signed-ce12-reductions}, define
\[
 x_{\mathrm{in}}:=R-\delta,\qquad
 h_{\mathrm{term}}:=\frac{k}{2R},\qquad
 d_3^C:=\min\left\{\frac{\alpha}{R},\frac{\delta}{W}\right\},
\]
and
\[
 \begin{aligned}
 c_0^{(5)}&:=k,&
 c_1^{(5)}&:=1-\delta/R,&
 c_2^{(5)}&:=1-\delta,&
 c_3^{(5)}&:=1-d_3^C,\\
 c_4^{(5)}&:=1-\alpha,&
 c_5^{(5)}&:=1-\alpha/W.
 \end{aligned}
\]
Define the formal incoming-reach iterates by
\[
 z_0:=x_{\mathrm{in}},\qquad
 z_j:=\widehat g_{c_j^{(5)}}^\vee(z_{j-1})
 \quad(1\le j\le5),
\qquad Z:=z_5.
\]
Equivalently,
\[
 Z=
 [\widehat g_{c_1^{(5)}}^\vee\mid
  \widehat g_{c_2^{(5)}}^\vee\mid
  \widehat g_{c_3^{(5)}}^\vee\mid
  \widehat g_{c_4^{(5)}}^\vee\mid
  \widehat g_{c_5^{(5)}}^\vee](x_{\mathrm{in}}).
\]
\end{definition}

\begin{definition}[Certified CE1 affine subdomain]
\label{def:body-ce1-affine-domain}
For $(R,\alpha,\delta)\in\mathcal D_{\rm CE1}$, set
\[
 m_3:=d_3^C,\qquad
 q_1:=h_{\mathrm{term}}+
 (1-4\alpha)(h_{\mathrm{term}}-\alpha).
\]
Define
\[
 \mathcal D_{\rm aff}:=
 \left\{(R,\alpha,\delta)\in\mathcal D_{\rm CE1}:
 \begin{array}{c}
 0<\delta<d_*,\quad 0<m_3<1/2,\\
 h_{\mathrm{term}}\in\mathcal T_\alpha,\quad
 q_1\in\mathcal T_{m_3},\quad x_{\mathrm{in}}>e(\delta),\\
 1-4\alpha\in\mathcal C_\alpha,\quad
 1-5m_3\in\mathcal C_{m_3}
 \end{array}
 \right\}.
\]
For $(R,\alpha,\delta)\in\mathcal D_{\rm aff}$, define
\[
 \ell_0:=h_{\mathrm{term}},\qquad
 \ell_1:=
 \widehat g_{1-\alpha}^{\vee,\,1-4\alpha}(\ell_0),
\qquad
 \ell_2:=
 \widehat g_{1-m_3}^{\vee,\,1-5m_3}(\ell_1).
\]
\end{definition}

\begin{figure}[H]
\centering
\resizebox{.98\linewidth}{!}{%
\input{figures/strategy2_affine_domain_skeleton}}
\caption{The certified CE1 double-selected-$T_+$ subdomain
$\mathcal D_{\rm aff}$ and its two retained affine states.}
\label{fig:strategy2-affine-domain-skeleton}
\end{figure}

\begin{proposition}[Five-V triangle one-gap certificate]
\label{prop:body-five-V-triangle-certificate}
Assume
\[
 (R,\alpha,\delta)\in
 \mathcal D_{\rm CE1}\cup\mathcal D_{\rm CE2},
\]
all six roles are Vd0, $N_+=1$, and the normalized right center trace
contains a V-gap.  If $(R,\alpha,\delta)\in\mathcal D_{\rm CE2}$, assume the
companion trace is gap-free.  Use the data of
Definition~\ref{def:body-five-V-triangle-data}.
The actual V triangles satisfy
\[
 A_0\ge Z,
 \qquad
 A_0<1-h_{\mathrm{term}}.
\]
Exact capped-map duality reduces the required contradiction
$Z>1-h_{\mathrm{term}}$ to
\[
 [\widehat g_{1-\alpha}^\vee
  \mid\widehat g_{1-d_3^C}^\vee
  \mid\widehat g_{1-\delta}^\vee](h_{\mathrm{term}})
 >1-x_{\mathrm{in}}.
\]
For $(R,\alpha,\delta)\in\mathcal D_{\rm aff}$, the two certified affine
iterates of Definition~\ref{def:body-ce1-affine-domain} satisfy
\[
 \ell_2>e(\delta),
\qquad
 \widehat g_{1-\delta}^{\vee,\mathrm{th}}(\ell_2)
 >1-x_{\mathrm{in}}.
\]
The remaining CE1 capped-map branches are closed by the exact branch audit.

For $(R,\alpha,\delta)\in\mathcal D_{\rm CE2}$,
\[
 0<\alpha<d_*,\qquad 0<\delta<d_*,
\]
\[
 x_{\mathrm{in}}>e(\alpha),\qquad
 \min\{e(\alpha),e(\delta)\}<h_{\mathrm{term}}.
\]
Therefore at least one of the $\alpha$- or $\delta$-threshold slots may be
chosen; every other slot is replaced by $\mathrm I$.  Both thresholds may be
available, and no uniqueness is asserted.  Reflection exchanges
$(R,\alpha)$ with $(W,\delta)$ and reverses the V-triangle order.
\end{proposition}

\begin{proof}
The actual-V triangle induction and terminal diameter bound are
Proposition~\ref{prop:signed-one-gap-interface}.  The CE1 affine calculation is
Proposition~\ref{prop:reader-ce1-one-gap}; the CE2 alternatives are
Proposition~\ref{prop:signed-ce2-one-gap}.  Every formal iterate is a lower
bound for the corresponding actual incoming reach.
\end{proof}

\begin{figure}[H]
\centering
\resizebox{.99\linewidth}{!}{\input{figures/strategy2_five_row_chain_skeleton}}
\caption{The exact one-gap five-V-triangle chain.  Every $z_j$ is a formal lower
bound for the incoming reach at the corresponding skeleton V triangle.}
\label{fig:strategy2-five-V-triangle-skeleton}
\end{figure}

\begin{figure}[H]
\centering
\resizebox{.99\linewidth}{!}{\input{figures/strategy2_relaxation_slots_skeleton}}
\caption{The affine slots certified on $\mathcal D_{\rm aff}$ and the two CE2
threshold alternatives.  In CE2 at least one threshold fires; uniqueness is
not used.}
\label{fig:strategy2-relaxation-slots-skeleton}
\end{figure}

\begin{definition}[Support-isolated T3-like endpoint state]
\label{def:body-t3-endpoint-state}
For $T\in\mathfrak E_1$ and $i\in\mathbb Z/6\mathbb Z$, define
\[
 o_H(T):=\left\lvert\mathrm{Vert}(T)\setminus H\right\rvert,
\qquad
 n_i(T):=
 \left\lvert
 \left\{\epsilon\in\{-1,1\}:
 \Haus(T\cap r_{i+\epsilon})>0\right\}
 \right\rvert.
\]
For $\mathbf T=(T_0,\ldots,T_5)\in\mathfrak E_1^6$, define
\[
 \mathfrak S_{\rm T3}:=
 \left\{
 \begin{array}{c}
 (R,\alpha,\delta,\mathbf T)\in
 (\mathcal D_{\rm CE1}\cup\mathcal D_{\rm CE2})
 \times\mathfrak E_1^6:\\
 V_j\in\interior(T_j)\quad\forall j,\qquad
 H\subset
 \interior(T_C(R,\alpha,\delta))
 \cup\displaystyle\bigcup_{j=0}^5\interior(T_j),\\
 A_j+B_j\le1\quad\forall j\in\mathbb Z/6\mathbb Z,\\
 T_C(R,\alpha,\delta)\cap\{M_0,\ldots,M_5\}=\{M_0\},\\
 (o_H(T_0),n_0(T_0))=(2,1),\quad
 \Haus(T_0\cap r_1)>0,\\
 (o_H(T_j),n_j(T_j))
 \in\{(1,0),(2,0),(3,0),(2,1)\}
 \quad\forall j,\\
 \Haus(T_j\cap r_1)>0\Longrightarrow j\in\{0,1\}
 \quad\forall j,\\
 \Haus(T_j\cap r_5)>0\Longrightarrow j=5
 \quad\forall j
 \end{array}
 \right\}.
\]
For $(R,\alpha,\delta,\mathbf T)\in\mathfrak S_{\rm T3}$, put
\[
 U_C:=\interior(T_C(R,\alpha,\delta))
\]
and define the nonempty majorant family
\[
 \mathfrak M_{\rm T3}(T_0):=
 \left\{
 \begin{array}{c}
 S\in\mathfrak E_1:\\
 \exists v\in\mathbb R^2,\ S=T_0+v,\qquad
 V_0\in\partial S\setminus\mathrm{Vert}(S),\\
 S\cap e_{5,0}=T_0\cap e_{5,0},\\
 T_0\cap H\subsetneq S\cap H,\qquad
 (o_H(S),n_0(S))=(2,1),\\
 \interior(T_0)\cap(H\setminus\{V_0\})
 \subset\interior(S)
 \end{array}
 \right\}
 =\{\widetilde T_0\}.
\]
Define
\[
\xi_+:[0,1]\to e_{0,1},\qquad
 \xi_+(t):=V_0+t(V_1-V_0),
\]
\[
\xi_-:[0,1]\to e_{5,0},\qquad
 \xi_-(t):=V_0+t(V_5-V_0),
\]
\[
 \rho_1:[0,1]\to r_1,\qquad
 \rho_1(t):=V_1+t(O-V_1).
\]
\[
 b_{01}:=
 \sup\{t\in[0,1]:\xi_+(t)\in\widetilde T_0\},
\qquad
 b_{05}:=
 \sup\{t\in[0,1]:\xi_-(t)\in\widetilde T_0\},
\]
\[
 r_{\mathrm{in}}:=
 \inf\{t\in[0,1]:\rho_1(t)\in\widetilde T_0\},
\qquad
 r_{\mathrm{out}}:=
 \sup\{t\in[0,1]:\rho_1(t)\in\widetilde T_0\}.
\]
Then
\[
 \widetilde T_0\cap e_{0,1}=\xi_+([0,b_{01}]),\qquad
 \widetilde T_0\cap e_{5,0}=\xi_-([0,b_{05}]),
\]
\[
 \widetilde T_0\cap r_1
 =\rho_1([r_{\mathrm{in}},r_{\mathrm{out}}]),
 \qquad r_{\mathrm{in}}\le r_{\mathrm{out}}.
\]
Define
\[
 J_+:=\mathrm{cl}_{[0,1]}
 \{t\in[0,1]:\xi_+(t)\in U_C\},
\qquad
 J_-:=\mathrm{cl}_{[0,1]}
 \{t\in[0,1]:\xi_-(t)\in U_C\},
\]
with
\[
 \mathrm{cl}_{[0,1]}(\varnothing):=\varnothing,
\qquad
 (J_+,J_-)\in\mathfrak J^2.
\]
\[
 a_1^\ast:=\mathcal R_{J_+}(b_{01}),\qquad
 a_5^\ast:=\mathcal R_{J_-}(b_{05}),
\]
\[
 c_5^\ast:=1-d_5^C,
\qquad
 c_1^\ast:=
 \begin{cases}
  r_{\mathrm{in}},
   &r_{\mathrm{in}}\le1-d_1^C\le r_{\mathrm{out}},\\
  1-d_1^C,&\text{otherwise}.
 \end{cases}
\]
These are the quantities audited in
Lemma~\ref{lem:407-four-label-loss}.
\[
 \mathfrak L_{\rm cap}:=
 \{\mathrm{Full},\mathrm L,T_-,T_+^{\rm hi}\}.
\]
\end{definition}

\begin{figure}[H]
\centering
\resizebox{.99\linewidth}{!}{%
\input{figures/strategy2_t3_state_domain_skeleton}}
\caption{The normalized mathematical state set $\mathfrak S_{\rm T3}$ and the
nonempty family $\mathfrak M_{\rm T3}(T_0)$ of admissible closed-trace
majorants.}
\label{fig:strategy2-t3-state-domain-skeleton}
\end{figure}

\begin{proposition}[T3-like endpoint certificate]
\label{prop:body-t3-endpoint}
For every $(R,\alpha,\delta,\mathbf T)\in\mathfrak S_{\rm T3}$, the endpoint
data associated with
$\mathfrak M_{\rm T3}(T_0)=\{\widetilde T_0\}$ satisfy
\[
 \widehat g_{c_5^\ast}(1-a_5^\ast)+
 \widehat g_{c_1^\ast}(1-a_1^\ast)<1.
\]
The ordered pair of endpoint labels lies in
$\mathfrak L_{\rm cap}^2$ and is audited exactly; no unsupported common
envelope replaces that audit.  The three internal nonsupercritical V triangles form
a path satisfying Definition~\ref{def:body-center-free-path} and are replaced
by $\mathrm I^3$.
\end{proposition}

\begin{proof}
This is Theorem~\ref{thm:paper-complete-t3-endpoint}.
\end{proof}

\begin{figure}[H]
\centering
\resizebox{.99\linewidth}{!}{\input{figures/strategy2_t3_residual_skeleton}}
\caption{The support-isolated T3-like V triangle, its two center-assisted boundary
residuals, and its two forced endpoint radial demands.}
\label{fig:strategy2-t3-residual-skeleton}
\end{figure}

\begin{definition}[Adjacent-rescuer data]
\label{def:body-adjacent-rescuer-data}
Define
\[
 \mathfrak I_{\rm res}:=
 \left\{
 \begin{array}{c}
 (T,a,c,u)\in\mathfrak E_1\times[0,1]^3:\\
 T\cap e_{5,0}=\xi_-([0,a]),\quad
 T\cap r_1=\rho_1([c,u]),\\
 \Haus(T\cap r_5)=0,\quad
 0\le c\le1/2\le u<1,\quad c<u
 \end{array}
 \right\},
\]
\[
 \mathfrak R_{\rm T3}:=
 \left\{
 \begin{array}{c}
 (T,a,c,u)\in\mathfrak I_{\rm res}:\\
 (o_H(T),n_0(T))=(2,1),\quad
 V_0\in\partial T\setminus\mathrm{Vert}(T)
 \end{array}
 \right\},
\]
\[
 \mathfrak R_{\rm Vd1}:=
 \left\{
 \begin{array}{c}
 (T,a,c,u)\in\mathfrak I_{\rm res}:\\
 (o_H(T),n_0(T))=(1,1),\quad V_0\in\interior(T)
 \end{array}
 \right\},
\]
\[
 \mathfrak R_{\rm res}:=
 \mathfrak R_{\rm T3}\cup\mathfrak R_{\rm Vd1},
\]
\[
 \mathfrak D_{\rm res}:=
 \left\{
 \begin{array}{c}
 (U,T,a,c,u)\in
 \mathcal P(\mathbb R^2)\times\mathfrak E_1\times[0,1]^3:\\
 \overline U\in\mathfrak E_1,\quad
 U=\interior(\overline U),\quad O\in U,\quad
 (T,a,c,u)\in\mathfrak R_{\rm res},\\
 \rho_1((u,1])\subset U
 \end{array}
 \right\}.
\]
For
\[
 (U_C,\widetilde T_{\mathrm{res}},
 a_{\mathrm{res}},c_{\mathrm{res}},u_{\mathrm{res}})
 \in\mathfrak D_{\rm res},
\]
define
\[
 \varepsilon_{\mathrm{res}}:=1-u_{\mathrm{res}}>0,
 \qquad
 \vartheta_{\mathrm{res}}
 :=\frac{a_{\mathrm{res}}}
 {a_{\mathrm{res}}+\varepsilon_{\mathrm{res}}}.
\]
\end{definition}

\begin{figure}[H]
\centering
\resizebox{.99\linewidth}{!}{%
\input{figures/strategy2_rescuer_domain_skeleton}}
\caption{The mathematical rescuer domain
$\mathfrak D_{\rm res}$: translated T3-like majorants and adjacent Vd1
closures enter one exact tuple interface.}
\label{fig:strategy2-rescuer-domain-skeleton}
\end{figure}

\begin{definition}[CE2 Vd residual data]
\label{def:body-vd-residual-data}
For $\mathbf T=(T_0,\ldots,T_5)\in\mathfrak E_1^6$, define
\[
 \mathfrak S_{\rm Vd}:=
 \left\{
 \begin{array}{c}
 (R,\alpha,\delta,\mathbf T,\tau)\in
 \mathcal D_{\rm CE2}\times\mathfrak E_1^6\times\{1,2,3,4,5\}:\\
 V_j\in\interior(T_j)\quad\forall j,\qquad
 H\subset
 \interior(T_C(R,\alpha,\delta))
 \cup\displaystyle\bigcup_{j=0}^5\interior(T_j),\\
 T_C(R,\alpha,\delta)\cap\{M_0,\ldots,M_5\}=\{M_0\},\\
 A_0+B_0>1,\qquad
 A_j+B_j\le1\quad\forall j\ne0,\\
 (o_H(T_\tau),n_\tau(T_\tau))\in\{(1,1),(1,2)\},\\
 (o_H(T_j),n_j(T_j))\in\{(1,0),(2,0),(3,0)\}
 \quad\forall j\ne\tau
 \end{array}
 \right\},
\]
\[
 \mathfrak S_{\rm Vd}^{\rm adj}:=
 \{(R,\alpha,\delta,\mathbf T,\tau)\in\mathfrak S_{\rm Vd}:
 \tau\in\{1,5\}\},
\]
\[
 \mathfrak S_{\rm Vd}^{\rm non}:=
 \{(R,\alpha,\delta,\mathbf T,\tau)\in\mathfrak S_{\rm Vd}:
 \tau\in\{2,3,4\}\}.
\]
For $(R,\alpha,\delta,\mathbf T,\tau)\in\mathfrak S_{\rm Vd}$, define
\[
 I_{\mathrm L}:=\left[\frac{k}{W},R+\alpha\right],
 \qquad
 I_{\mathrm R}:=\left[\frac{k}{R},W+\delta\right],
\]
\[
 (I_{\mathrm L},I_{\mathrm R})\in\mathfrak J^2,
\]
\[
 a_{\mathrm R}^{\mathrm{res}}
 :=\mathcal R_{I_{\mathrm R}}(B_0),
 \qquad
 a_{\mathrm L}^{\mathrm{res}}
 :=\mathcal R_{I_{\mathrm L}}(A_0),
 \qquad
 s_C:=\alpha+\delta.
\]
For $(R,\alpha,\delta,\mathbf T,\tau)\in
\mathfrak S_{\rm Vd}^{\rm adj}$, define
\[
 \kappa_\tau:=2\tau\pmod 6.
\]
\[
 U_{\tau\to\kappa_\tau}:=
 \sup\left(
 \{0\}\cup
 \{t\in[0,1]:
 V_{\kappa_\tau}+t(O-V_{\kappa_\tau})\in T_\tau\}
 \right)\in[0,1].
\]
For $(R,\alpha,\delta,\mathbf T,\tau)\in
\mathfrak S_{\rm Vd}^{\rm non}$, define
\[
 C_\tau:=
 \sup\{t\in[0,1]:V_\tau+t(O-V_\tau)\in T_\tau\}.
\]
\end{definition}

\begin{figure}[H]
\centering
\resizebox{.99\linewidth}{!}{%
\input{figures/strategy2_vd_state_domain_skeleton}}
\caption{The exact CE2 state domain
$\mathfrak S_{\rm Vd}=\mathfrak S_{\rm Vd}^{\rm adj}
\sqcup\mathfrak S_{\rm Vd}^{\rm non}$ for the adjacent and nonadjacent
one-Vd residual certificates.}
\label{fig:strategy2-vd-state-domain-skeleton}
\end{figure}

\begin{definition}[Vd1--supercritical two-chart replacement data]
\label{def:body-vd-replacement-data}
For an adjacent pair based at $V_0,V_1$, define the separate local charts
\[
 X_0(x,y)=V_0+x(V_5-V_0)+y(V_1-V_0),
\qquad
 X_1(x,y)=V_1+x(V_0-V_1)+y(V_2-V_1).
\]
The replacement parameters $p_1,p_2,\varepsilon$ are selected from the strict
Vd1, supercritical, and center-handoff margins proved in
Proposition~\ref{prop:paper-vd1-pair-replacement}. The $V_0$ replacement is a
shifted minus-axis template in the chart $X_0$; the $V_1$ replacement is the
appropriate shifted minus- or plus-axis template in the chart $X_1$.
\end{definition}

\begin{proposition}[Adjacent rescuer and Vd terminal certificates]
\label{prop:body-special-terminal-certificates}
The following exact geometric terminals hold.
\begin{enumerate}
\item For the data of Definition~\ref{def:body-adjacent-rescuer-data}, the
local profile gives
\[
 a_{\mathrm{res}}\le1-g_{c_{\mathrm{res}}}^{\rm sc},
 \qquad
 \vartheta_{\mathrm{res}}\le1-g_{c_{\mathrm{res}}}^{\rm sc}.
\]
Radial isolation forces the opposite boundary demand to be at least
$g_{c_{\mathrm{res}}}^{\rm sc}$, whereas the unique supercritical V triangle has
outgoing reach strictly below $g_{c_{\mathrm{res}}}^{\rm sc}$.  The
intervening four-V-triangle boundary path is center-free on its internal edges, so
Lemma~\ref{lem:reader-boundary-path} gives a contradiction.

\item In the adjacent CE2 one-Vd1/Vd2 placement, use
Definition~\ref{def:body-vd-residual-data} and reflect the
$\tau=5$ case to the normalized representative $\tau=1$.  Then
\[
 a_{\mathrm R}^{\mathrm{res}}+a_{\mathrm L}^{\mathrm{res}}<\frac12,
 \qquad
 \delta<\frac{a_{\mathrm L}^{\mathrm{res}}}{4}.
\]
More precisely,
\[
 U_{1\to2}<1-a_{\mathrm L}^{\mathrm{res}},
 \qquad
 C_2\le1-\frac{a_{\mathrm L}^{\mathrm{res}}}{4}
 <1-\delta.
\]
The first bound also lies below $1-\delta$; hence $r_2$ is uncovered.

\item In a nonadjacent placement, use
Definition~\ref{def:body-vd-residual-data}.  Then
\[
 a_{\mathrm R}^{\mathrm{res}}>s_C,\qquad
 a_{\mathrm L}^{\mathrm{res}}>s_C,
\]
\[
 d_\tau^C<
 \min\{a_{\mathrm R}^{\mathrm{res}},a_{\mathrm L}^{\mathrm{res}}\},
 \qquad
 C_\tau<
 1-\min\{a_{\mathrm R}^{\mathrm{res}},a_{\mathrm L}^{\mathrm{res}}\}.
\]
Thus the Vd radial trace and the center trace are disjoint.

\item In the remaining adjacent Vd1--supercritical placement, use
Definition~\ref{def:body-vd-replacement-data}.  The two replacement roles are open nonsupercritical Vd0 roles, are constructed in separate vertex charts, and preserve coverage of the full skeleton. The skeleton-level all-Vd0 theorem then applies.
\end{enumerate}
\end{proposition}

\begin{proof}
Items 1--3 are Corollary~\ref{cor:signed-adjacent-rescuers} and
Lemmas~\ref{lem:signed-vd-adjacent-placement} and
\ref{lem:signed-vd-nonadjacent-placement}.  Item 4 is
Proposition~\ref{prop:paper-vd1-pair-replacement}.
\end{proof}

\begin{figure}[H]
\centering
\resizebox{.99\linewidth}{!}{\input{figures/strategy2_rescuer_skeleton}}
\caption{The common adjacent-rescuer interface for a T3-like or Vd1 role and
the unique strict-supercritical V triangle.}
\label{fig:strategy2-rescuer-skeleton}
\end{figure}

\begin{figure}[H]
\centering
\resizebox{.99\linewidth}{!}{\input{figures/strategy2_vd_placements_skeleton}}
\caption{The adjacent and nonadjacent CE2 one-Vd placements.  The displayed
strict inequalities separate the vertex-side and center-side radial traces.}
\label{fig:strategy2-vd-placements-skeleton}
\end{figure}

\begin{figure}[H]
\centering
\resizebox{.99\linewidth}{!}{\input{figures/strategy2_vd_replacement_skeleton}}
\caption{The remaining Vd1--supercritical pair and its two open,
nonsupercritical Vd0 axis replacements.  All local-coordinate formulas are
exact; placement is schematic.}
\label{fig:strategy2-vd-replacement-skeleton}
\end{figure}

\begin{definition}[Full six-slot V-triangle chain]
\label{def:body-six-slot-chain}
For each $i\in\mathbb Z/6\mathbb Z$, let
\[
 J_i^C:=\xi_i^{-1}(T_C\cap e_{i,i+1})\in\mathfrak J
\]
be the scalar center interval on the outgoing edge of $T_i$.  Define the exact
raw and nonsupercritical V-triangle transfers
\[
 \Phi_i^{\rm raw}:=g_{c_i,J_i^C}^{\vee},
 \qquad
 \Phi_i^{\rm ns}:=\widehat g_{c_i,J_i^C}^{\vee}.
\]
The full cyclic chain is always written with six explicit slots in geometric
V-triangle order:
\[
 [\Phi_0\mid\Phi_1\mid\Phi_2\mid\Phi_3\mid\Phi_4\mid\Phi_5].
\]
If $T_i$ is nonsupercritical and the outgoing edge is an ordinary
center-free handoff, then $J_i^C=\varnothing$ and
\[
 A_{i+1}\ge\widehat g_{c_i}^{\vee}(A_i)\ge A_i.
\]
Only under this condition, together with exclusion of nonincident boundary
traces on that edge, may the $i$th slot be replaced by $\mathrm I$ in a
certificate word.  Such a replacement means that the exact handoff inequality
\[
 A_{i+1}\ge\widehat g_{c_i}^{\vee}(A_i)
\]
has been weakened to $A_{i+1}\ge A_i$.  It is a statement about that
individual handoff; no pointwise order between the two formal compositions is
being asserted.  The terminal proposition named in the register supplies the
valid composition/endpoint argument for the whole certificate word.
\end{definition}

\begin{table}[H]
\centering
\scriptsize
\renewcommand{\arraystretch}{1.18}
\begin{tabular}{@{}p{.20\textwidth}p{.69\textwidth}@{}}
\toprule
branch&six explicitly displayed functions and the certified handoff word\\
\midrule
$N_+=0$, all Vd0, $\mathrm{gr}=0$
&The branch exclusions give $J_i^C=\varnothing$ for every $i$.  The exact word
is
$\mathcal G_{00}=[\widehat g_{c_0,J_0^C}^{\vee}\mid
\widehat g_{c_1,J_1^C}^{\vee}\mid\widehat g_{c_2,J_2^C}^{\vee}\mid
\widehat g_{c_3,J_3^C}^{\vee}\mid\widehat g_{c_4,J_4^C}^{\vee}\mid
\widehat g_{c_5,J_5^C}^{\vee}]$.
The certified weaker handoffs are recorded as
$\mathcal C_{00}=[\mathrm I\mid\mathrm I\mid\mathrm I\mid
\mathrm I\mid\mathrm I\mid\mathrm I]$; strict cyclic ascent gives the
contradiction.\\
\addlinespace
$N_+=0$, all Vd0, $\mathrm{gr}=1$
&After cyclic normalization $J_0^C$ is the positive-length center trace; every
possible point contact remains explicit in
$\mathcal G_{01}=[\widehat g_{c_0,J_0^C}^{\vee}\mid
\widehat g_{c_1,J_1^C}^{\vee}\mid\widehat g_{c_2,J_2^C}^{\vee}\mid
\widehat g_{c_3,J_3^C}^{\vee}\mid\widehat g_{c_4,J_4^C}^{\vee}\mid
\widehat g_{c_5,J_5^C}^{\vee}]$.
The certificate word is
$\mathcal C_{01}=[\widehat g_{c_0,J_0^C}^{\vee}\mid
\widehat g_{c_1,J_1^C}^{\vee}\mid\mathrm I\mid\mathrm I\mid\mathrm I\mid
\widehat g_{c_5,J_5^C}^{\vee}]$; the one-gap endpoint sum is $<1$.\\
\addlinespace
CE2, $\mathrm{gr}=2$
&Normalize the two positive-length traces as $J_0^C,J_5^C$ and retain all
point contacts in
$\mathcal G_{02}=[\widehat g_{c_0,J_0^C}^{\vee}\mid
\widehat g_{c_1,J_1^C}^{\vee}\mid\widehat g_{c_2,J_2^C}^{\vee}\mid
\widehat g_{c_3,J_3^C}^{\vee}\mid\widehat g_{c_4,J_4^C}^{\vee}\mid
\widehat g_{c_5,J_5^C}^{\vee}]$.
The certificate word is
$\mathcal C_{02}=[\widehat g_{c_0,J_0^C}^{\vee}\mid
\widehat g_{c_1,J_1^C}^{\vee}\mid\mathrm I\mid\mathrm I\mid\mathrm I\mid
\widehat g_{c_5,J_5^C}^{\vee}]$; the paired endpoint sum is $<1$.\\
\addlinespace
$N_+=0$, one T3-like V triangle
&For support-isolated $T_0$ the exact word is
$\mathcal G_{\rm T3}=[\widehat g_{c_0,J_0^C}^{\vee}\mid
\widehat g_{c_1,J_1^C}^{\vee}\mid\widehat g_{c_2,J_2^C}^{\vee}\mid
\widehat g_{c_3,J_3^C}^{\vee}\mid\widehat g_{c_4,J_4^C}^{\vee}\mid
\widehat g_{c_5,J_5^C}^{\vee}]$.
The certificate word is
$\mathcal C_{\rm T3}=[\widehat g_{c_0,J_0^C}^{\vee}\mid
\widehat g_{c_1,J_1^C}^{\vee}\mid\mathrm I\mid\mathrm I\mid\mathrm I\mid
\widehat g_{c_5,J_5^C}^{\vee}]$; the audited four-label endpoint sum is $<1$.\\
\addlinespace
$N_+=1$, all Vd0, one gap
&With $T_0$ the unique strictly supercritical V triangle, the full exact word is
$\mathcal G_{11}=[g_{c_0,J_0^C}^{\vee}\mid
\widehat g_{c_1,J_1^C}^{\vee}\mid\widehat g_{c_2,J_2^C}^{\vee}\mid
\widehat g_{c_3,J_3^C}^{\vee}\mid\widehat g_{c_4,J_4^C}^{\vee}\mid
\widehat g_{c_5,J_5^C}^{\vee}]$.
The proof retains the exact five-slot hatted subchain on $T_1,\ldots,T_5$ and
closes the $T_0$ slot by $A_0<1-h_{\mathrm{term}}<Z$.\\
\bottomrule
\end{tabular}
\caption{The reader-facing Strategy~2 $g$-composition register.  Every chain
shows all six functions.  An $\mathrm I$ records the weaker handoff
$A_{i+1}\ge A_i$ under Definition~\ref{def:body-six-slot-chain}; it is not an
informal placeholder or an unproved order relation between compositions.}
\label{tab:body-strategy2-chains}
\end{table}

\begin{center}
\small
\begin{tabular}{p{.36\textwidth}p{.52\textwidth}}
\toprule
non-composition terminal&certificate\\
\midrule
one T3-like or adjacent Vd1 rescuer&strict-supercritical envelope versus an
ordinary center-free boundary path\\
adjacent Vd1/Vd2&two center residuals and the quarter-radial envelope\\
nonadjacent Vd1/Vd2&two center residuals and the Vd corner margin\\
Vd1--supercritical pair&two explicit axis replacements followed by the
all-Vd0 contradiction\\
\bottomrule
\end{tabular}
\end{center}

\begin{proposition}[Branches closed by Strategy 2]
\label{prop:body-demand-branches}
Strategy~2 closes:
\begin{enumerate}
\item the $N_+=0$ all-Vd0 and T3-like-without-Vd1/Vd2 branches for CE1 and CE2;
\item every nonempty-gap $N_+=1$ all-Vd0 state;
\item the exactly-one-T3-like $N_+=1$ state with no Vd1/Vd2 role;
\item the complementary transfer placements in the CE2, $N_+=1$,
exactly-one-Vd1/Vd2 branch.
\end{enumerate}
\end{proposition}

\begin{proof}
Use Propositions~\ref{prop:body-all-vd0-endpoints},
\ref{prop:body-five-V-triangle-certificate}, \ref{prop:body-t3-endpoint}, and
\ref{prop:body-special-terminal-certificates}.  The complete CE2 one-Vd
placement assembly is Proposition~\ref{prop:paper-ce2-one-vd-placements}.
All singleton-gap states are included.
\end{proof}
