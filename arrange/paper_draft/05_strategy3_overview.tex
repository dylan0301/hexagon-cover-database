\section{Strategy 3: area loss}
\label{sec:area}

Normalize the area of a unit equilateral triangle to one.  Then
$\mathrm{area}(H)=6$.  At a vertex, affine wedge coordinates identify
$T_i\cap H$ with the portion of $T_i$ inside the first quadrant.  For lower
boundary reaches $a,b$, the local square-loss theorem gives
\[
 \mathrm{loss}(T_i)\ge\min(a,b)^2,
 \qquad
 a+b>1\Longrightarrow
 \mathrm{loss}(T_i)\ge\max(a,b)^2.
\]
A T3-like row has the separate bound
\[
 a,b\ge m\Longrightarrow
 \mathrm{loss}(T_i)\ge2m-4m^2.
\]
Their coordinate proofs, with every orientation retained, are in
Appendix~\ref{app:strategy3-details}.

\begin{figure}[H]
\centering
\input{figures/strategy3_area_loss}
\caption{Schematic local and cyclic views of Strategy~3, not to scale.  The
shaded regions suggest the forced exterior loss; the exact inequalities are
proved independently in Appendix~\ref{app:strategy3-details}.}
\label{fig:area-loss}
\end{figure}

\subsection{The two global sums}

Strict handoffs have $(a_i,b_i)=(1-x_{i-1},x_i)$, and selected
supercritical rows are precisely the cyclic ascents $x_i>x_{i-1}$.  If there
are at least two ascents, let $m=\min_i x_i$ and reflect so that
$m\le1-\max_i x_i$.  Four ordinary rows lose at least $m^2$, a row leaving
the minimum loses at least $(1-m)^2$, and a second ascent loses strictly more
than $1/4$.  Hence the total loss is greater than
\[
 4m^2+(1-m)^2+\frac14
 =5\left(m-\frac15\right)^2+\frac{21}{20}>1.
\]

If there is one ascent and a T3-like row, the supercritical row loses at
least $(1-m)^2$, the T3-like row at least $2m-4m^2$, and the other four rows
at least $m^2$ each.  Their total is
\[
 (1-m)^2+(2m-4m^2)+4m^2=1+m^2>1.
\]
The six vertex roles therefore contribute total inside area less than five;
adding the center role still gives less than the required six.

\begin{proposition}[Strategy 3 interface]
\label{prop:area-interface}
Strategy~3 closes the two rows of Table~\ref{tab:routing} assigned to route
$3$: CE0 with $N_+\ge2$, and CE0 with $N_+=1$, no Vd1/Vd2 role, and at least
one T3-like role.
\end{proposition}

\begin{proof}
The strict-handoff theorem transfers the actual $N_+$ pattern to the selected
ascents.  The two sums above are the global certificate.  Appendix
Proposition~\ref{prop:area-branches} verifies the local hypotheses and the
strict area normalization in both branches.
\end{proof}
