\section{Completion of the Proof}
\label{sec:assembly}

\begin{proof}[Proof of Theorem~\ref{thm:main}]
Assume that seven open unit equilateral triangles cover $H$.  By
Proposition~\ref{prop:exhaustive-structural-reduction}, the cover lies in
exactly one row of Table~\ref{tab:routing}.

First suppose $N_{\mathrm{gap}}=0$. The $N_+=0$ row is excluded by
Proposition~\ref{prop:length-branches}, and the $N_+\ge2$ row by
Proposition~\ref{prop:area-branches}. The entire $N_+=1$ row, without
any V-type restriction, is excluded by the type-independent nine-point
Proposition~\ref{prop:reader-ab-core-branches}. Its common-pair radial
forcing accounts for every permitted adjacent crossing; its three
asymmetric witnesses and exact terminal certificate are unchanged.

Now suppose $N_{\mathrm{gap}}\ge1$.  The rows assigned to trace length are
excluded by Proposition~\ref{prop:length-branches}.  Every nonzero-gap row
assigned to finite enclosure is excluded by the finite residual-hull theorem
Proposition~\ref{prop:finite-enclosure-nonzero-gap-branches}.  In the final
CE2 one-$\Vdone/\Vdtwo$ row, the trace-length complements and the
finite-enclosure placements are assembled in
Proposition~\ref{prop:new-one-vd-assembly}; its replacement subcase
recomputes the gap rank before selecting the closing strategy.  Thus every
zero- and nonzero-gap row is impossible, contradicting the assumed cover.
\end{proof}

Corollary~\ref{cor:expanded-closed} follows from
Proposition~\ref{prop:open-closed-scaled}.
