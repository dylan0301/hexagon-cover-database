% Historical text retained outside the canonical input closure.
Finally recompute the output gap rank from the modified open traces:
\[
 N'_{\rm gap}:=
 \left|\left\{i\in\mathbb Z/6\mathbb Z:
 e_{i,i+1}\setminus
 \bigl((U'_i\cap e_{i,i+1})\cup(U'_{i+1}\cap e_{i,i+1})\bigr)
 \ne\varnothing\right\}\right|.
\]
Then \(N'_{\rm gap}\in\{0,1,2\}\).  No equality with the input gap rank is
used.  If \(N'_{\rm gap}=0\), apply the boundary-complete length
row of \zcref{prop:length-branches}.  If \(N'_{\rm gap}=1\), the primed
actual reaches satisfy
\zcref{lem:new-one-gap-common-pair-adapter}, so the row-A branch of
\zcref{prop:new-nplus-zero-gap-closures} applies.  If
\(N'_{\rm gap}=2\), the primed actual reaches satisfy
\zcref{lem:new-two-gap-common-pair-adapter}, so the row-B branch of the same
proposition applies.  These alternatives are exhaustive.
