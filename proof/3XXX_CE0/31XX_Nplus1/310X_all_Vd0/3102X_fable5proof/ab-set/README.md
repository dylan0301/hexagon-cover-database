# The ab-set

An exact, case-by-case description of the **ab-set** with a rigorous proof and machine
verification.

## Problem

Let $O=(0,0)$ be the origin, $B=(b,0)$ a point on the positive $x$-axis, and
$A=a\,(\cos120^\circ,\sin120^\circ)$, so that $\angle AOB=120^\circ$ (counterclockwise, $A$ in the
second quadrant), with $|AO|=a\ge0$ and $|BO|=b\ge0$. Let $C$ be the closed $120^\circ$ convex cone
generated by the rays $OB$ and $OA$.

> **The ab-set** is
> $$K(a,b)=\Bigl(\bigcup\{\,T:\ T\ \text{a unit equilateral triangle with }\{A,O,B\}\subset T\,\}\Bigr)\cap C.$$

The goal is to describe $K(a,b)$ for **all** $a,b\ge0$: break the parameter quadrant into cases
given by polynomial inequalities, and for each case give the boundary of $K(a,b)$ as a list of
algebraic curves in counterclockwise order — with proof.

## The answer, in one paragraph

Write $\sigma=a+b$, $R=\sqrt{a^2+ab+b^2}=|AB|$, $h=\tfrac{\sqrt3}{2}$. A finite set fits in a unit
equilateral triangle of orientation $\varphi$ **iff** the sum of its three support values (in the
directions $\varphi,\varphi+120^\circ,\varphi+240^\circ$) is $\le h$ — a Viviani-type criterion.
This turns $K$ into a union of triangles over the admissible orientations, and its boundary is
traced by a **finite catalog** of curves:

* $K(a,b)=\varnothing$ exactly when $R>1$; and $K$ is a single triangle $\cap\,C$ when $R=1$;
* otherwise the boundary consists of: the two **cone edges**; **circles of radius 1** centered at
  $A$ and at $B$; four **limaçon quartics** $E_{AO},E_{BO},E_{BA},E_{AB}$ (the locus of the free
  vertex $x$ as a unit triangle pivots with one of $A,O,B$ on a side through $x$ and another on the
  opposite side); six **flush lines** ($L_A,L_B,M_A,M_B,\lambda_A,\lambda_B$, from a triangle side
  lying along $OA$ or $OB$); and **extreme-orientation triangle-side segments**.

The cases are organized into four **bands** by the topology of the admissible-orientation set
$\Phi=\{\varphi:F(\varphi)\le h\}$:

| band | inequalities | admissible orientations |
|---|---|---|
| I   | $4(a+b)^2\le3$ | all of them |
| II  | $4(a+b)^2>3,\ 4R^2\le3$ | one gap ("outer gap") |
| III | $4R^2>3,\ a+b\le1$ | two gaps ("outer" + "mid") |
| IV  | $a+b>1,\ R^2\le1$ | a single arc |

Within each band, finer structure is decided by explicit polynomial thresholds ($a=\tfrac12$,
$b=\tfrac12$, $a+b=\tfrac12$; flank cubics $w_\pm$; the flush-line quintic $\mathcal G$; the mid-dip
condition $(3+a)R^2=3$; and a resultant threshold $\mathcal E$), giving about twenty sub-cases in
total (plus mirror images under $a\leftrightarrow b$). The full case tables, with each boundary
written as an explicit counterclockwise list of the catalog curves, are in
[`ab_set.md`](ab_set.md).

## Files

| file | contents |
|---|---|
| [`ab_set.md`](ab_set.md) | **The result.** Curve catalog (§1), corner points (§2), sub-case predicates (§3), the case tables (§4), degenerate parameters (§5), worked examples (§6). |
| [`ab_set_proofs.md`](ab_set_proofs.md) | **The proof.** Viviani fitting criterion; the support/orientation function $F$ and existence ($\min_\varphi F=\tfrac{\sqrt3}2R$); star-shapedness and the radial reduction; classification of all boundary configurations into the catalog; derivation of each curve; corner and threshold identities; per-case lower-bound (realizability) and upper-bound (maximality) arguments; comprehensiveness of the case split. |
| [`ab_set_proofs.pdf`](ab_set_proofs.pdf) | Typeset PDF of the proof (10 pp., LaTeX math, linked table of contents). Rebuild with `sh build_proofs_pdf.sh` (needs `pandoc` + `tectonic`; the helper `pdf_preprocess.py` / `pdf_header.tex` handle the LaTeX preprocessing). |
| [`ab_set_verification.md`](ab_set_verification.md) | **Verification summary:** method, region audit, and the per-case results table. |
| [`verify_ab_set.py`](verify_ab_set.py) | Self-contained numerical certifier: an exact per-orientation radial-boundary solver (ground truth from the support-function criterion) compared against the claimed curves and arc order, for a sample of every case region. |
| [`derive_quartics.py`](derive_quartics.py) | `sympy` derivation of the parametric and implicit forms of the four limaçon quartics. |
| [`derive_thresholds.py`](derive_thresholds.py) | `sympy` derivation/verification of the corner points and the sub-case threshold polynomials. |

## The hexagonal hole set (second theorem)

Building on the ab-set catalog, the repository also proves that the **hexagonal hole set** — the
regular hexagon minus six ab-sets $K(a_i, 1-a_{i-1})$ placed at its vertices, under the standing
assumptions $a_0 > a_1 > \dots > a_5$ and $R_0^2 \le 1$ — **cannot be covered by any unit
equilateral triangle** ($F_S(\varphi) > h$ for every orientation $\varphi$). The proof is a
branch-and-bound tree over a two-parameter reduction $(s,t) = (a_5, a_0)$ (a monotonicity lemma
collapses the six parameters, and a mirror symmetry halves the region), with every leaf discharged
by exact positivity certificates over $\mathbb Q(\sqrt3)$.

| file | contents |
|---|---|
| [`hole_set_noncover.md`](hole_set_noncover.md) | The theorem, the reduction/mirror/arc/selection lemmas, the branch tree, the corner-leaf witnesses, and the equality-corner analysis. |
| [`hole_lab.py`](hole_lab.py) | Numerical laboratory: hole membership, boundary tracing, $F_S$ computation (used for design and cross-checks). |
| [`hole_cert_lib.py`](hole_cert_lib.py), [`hole_cert_gen.py`](hole_cert_gen.py) | Exact frames/witnesses and the certificate machinery (LP search + exact Handelman-style reconstruction). |
| [`gen_corner.py`](gen_corner.py), [`gen_bulk.py`](gen_bulk.py) | Certificate generators (corner leaves / bulk boxes) → `hole_certificates_corner.py`, `hole_certificates_bulk.py`. |
| [`verify_hole_noncover.py`](verify_hole_noncover.py) | Standalone exact verifier: re-derives every claim from the definitions and re-checks all certificates. |

```sh
python3 verify_hole_noncover.py            # exact re-verification of the hole-set theorem
python3 verify_hole_noncover.py --numeric  # + numerical defense-in-depth
```

## Reproducing the verification

```sh
python3 verify_ab_set.py          # full run (region audit + corner identities + per-case table)
python3 verify_ab_set.py quick    # faster, lower-resolution pass
python3 derive_quartics.py        # print the quartic curve equations
python3 derive_thresholds.py      # print the exact threshold/tangency polynomials
```

Requires `numpy` and `sympy`. The certifier computes the boundary from first principles (no use of
the claimed answer) and checks that, for each sampled direction, the boundary point lies on the
curve claimed for that arc and that the arcs appear in the stated counterclockwise order.

## Status

The three documents give the complete case-by-case answer with proofs. Every case region — all of
bands I–IV, the empty case $R>1$, the thin sliver sub-cases, and the degenerate axes $a=0$/$b=0$
and $R=1$ — is confirmed by `verify_ab_set.py`: the from-first-principles boundary matches the
claimed arc sequence and each sampled boundary point lies on its claimed curve at machine precision
(residuals $\sim10^{-15}$). See [`ab_set_verification.md`](ab_set_verification.md) for the per-case
table.
